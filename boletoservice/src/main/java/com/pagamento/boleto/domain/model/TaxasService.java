package com.pagamento.boleto.domain.model;

import com.pagamento.boleto.domain.model.Boleto;

public class TaxasService {
    public void aplicarTaxasEmissao(Boleto boleto) {
        // Cálculo de taxas para emissão
    }
    
    public void aplicarTaxasReemissao(Boleto boleto) {
        // Cálculo de taxas para reemissão
    }
}